# Исправление проблемы с Push уведомлениями

## Проблема

В логах сервера видно:
```
WARNING: Для пользователя bb4ba067-1499-496c-bd4d-c27f43dab83e нет зарегистрированных устройств!
WARNING: Список токенов пуст
```

Это означает, что устройство **не зарегистрировано** на сервере.

## Решение

### В Flutter приложении:

**ВАЖНО:** `FCMService.initialize(userId)` должен быть вызван **ОДИН РАЗ** при запуске приложения или входе пользователя, **НЕ** при каждом открытии чата!

#### Правильный способ:

```dart
// В главном экране или при входе пользователя
void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  
  await Firebase.initializeApp(
    options: DefaultFirebaseOptions.currentPlatform,
  );
  
  // Получаем userId из вашей системы аутентификации
  String userId = await getUserIdFromAuth(); // Ваша функция
  
  // Инициализируем FCM ОДИН РАЗ при запуске
  await FCMService.initialize(userId);
  
  runApp(MyApp());
}
```

#### Или в главном экране:

```dart
class HomeScreen extends StatefulWidget {
  @override
  _HomeScreenState createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  String userId = 'bb4ba067-1499-496c-bd4d-c27f43dab83e'; // Тот же ID что при отправке сообщений
  
  @override
  void initState() {
    super.initState();
    // Инициализируем FCM ОДИН РАЗ при открытии главного экрана
    _initializeFCM();
  }
  
  Future<void> _initializeFCM() async {
    try {
      await FCMService.initialize(userId);
      print('✅ FCM инициализирован для userId: $userId');
    } catch (e) {
      print('❌ Ошибка инициализации FCM: $e');
    }
  }
  
  // ... остальной код
}
```

### Критически важно:

1. **userId должен быть ТОЧНО таким же:**
   - При вызове `FCMService.initialize(userId)`
   - При отправке сообщений `SupportService.sendMessage(userId: userId, ...)`
   - При открытии чата `SupportChatScreen(userId: userId, ...)`

2. **Проверьте логи Flutter:**
   При запуске приложения должны появиться:
   ```
   🔔 Инициализация FCM для userId: bb4ba067-1499-496c-bd4d-c27f43dab83e
   📱 Разрешение на уведомления: AuthorizationStatus.authorized
   🔑 FCM Token получен: ...
   📤 Регистрация устройства: userId=..., platform=android
   ✅ Устройство успешно зарегистрировано на сервере
   ```

3. **Проверьте логи сервера:**
   После регистрации должно быть:
   ```
   INFO: ✅ Устройство успешно зарегистрировано для пользователя bb4ba067-1499-496c-bd4d-c27f43dab83e
   INFO:    Platform: android, Token: ...
   INFO:    Всего устройств для пользователя: 1
   ```

## Проверка регистрации

После запуска приложения проверьте:

```bash
curl http://192.168.0.34:5001/check_device/bb4ba067-1499-496c-bd4d-c27f43dab83e
```

Должно вернуть:
```json
{
  "user_id": "bb4ba067-1499-496c-bd4d-c27f43dab83e",
  "registered": true,
  "device_count": 1,
  "has_tokens": true
}
```

Если `registered: false` - устройство не зарегистрировано!

## После регистрации

Когда устройство зарегистрировано, push уведомления будут приходить:
- ✅ Даже если пользователь не в чате
- ✅ Когда приложение закрыто
- ✅ Когда приложение в фоне
- ✅ Когда приложение открыто (через WebSocket или push)

## Типичные ошибки

1. **FCM инициализируется в SupportChatScreen** - НЕПРАВИЛЬНО!
   - Должно быть в главном экране или при входе

2. **Разные userId** - НЕПРАВИЛЬНО!
   - userId должен быть одинаковым везде

3. **Не вызывается FCMService.initialize()** - НЕПРАВИЛЬНО!
   - Должен вызываться обязательно

4. **Разрешения не предоставлены** - проверьте логи Flutter

